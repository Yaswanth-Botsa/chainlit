services:
  - type: web
    name: chainlit
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: chainlit run app.py --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false